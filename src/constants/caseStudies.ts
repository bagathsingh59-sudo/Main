import type { CaseStudy } from "@/types";

/**
 * Case studies are intentionally empty until real, anonymised client
 * stories are written and approved. The CaseStudies section auto-hides
 * when this array is empty — visitors see no placeholder rows.
 *
 * To add one: copy the CaseStudy type and write a real story with the
 * client name anonymised (e.g. "A 90-employee manufacturing client in
 * Karnataka") — never invent a brand name.
 */
export const CASE_STUDIES: CaseStudy[] = [];
